import { RoundRatingPipe } from './round-rating.pipe';

describe('RoundRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
